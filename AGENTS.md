# 幕末人物・勢力ナビ

## プロジェクトの目的

幕末の人物、勢力、事件、場所、関係の変化を短時間でつかめる、ビジュアルな歴史ナビゲーションを作る。
歴史に詳しい人が暗黙に持っている背景知識へ、文章量ではなく見やすい状況整理と操作を通じて近づけることを目的とする。

利用者がサイトを必要とするきっかけや閲覧前後の文脈は、公開画面、README、メタ情報のキャッチコピーとして直接説明しない。公開文言は、人物・勢力・事件・場所とその変化を理解するサイトとして表現する。

特定の漫画、ドラマ、小説などの内容を解説するサイトではない。
作品名、作中描写、巻数、ネタバレ情報はデータや画面に含めない。

## 最重要の設計原則

- 初見の人が選択時点の事件、主要人物、勢力関係を10秒程度で把握できることを最優先とする。
- 人物・勢力・事件の追加より、視線誘導、比較、図解、スマートフォンでの読みやすさを優先する。
- 詳細な出典整備を目的化せず、利用者が背景を理解するために必要な情報へ制作時間を配分する。
- 人物を生涯の固定プロフィールとして扱わない。
- 選択時点での名前、所属、役職、立場を表示する。
- 人物関係は固定せず、開始時点と終了時点を持たせる。
- 幕府対倒幕の二項対立だけで人物を分類しない。
- 同じ藩や組織内の意見の違いも表現する。
- 事実と解釈を区別し、作品上の設定を史実として混ぜない。
- 不明な情報は、画面を埋めるためだけに推測で補完しない。
- 諸説ある情報は `disputed` または `needs_review` として扱う。
- 歴史的事実を変更・追加する場合は出典を追加する。
- Wikipediaだけを最終根拠にしない。

## 優先する情報源

出典調査は正確さの最低限の防波堤として行い、全項目への精密な頁・コマ情報追加は優先タスクにしない。新しい歴史的事実を追加・変更するときは、表示内容を支える信頼できる資料を少なくとも1件登録する。

1. 公文書・一次資料
2. 国立公文書館
3. 国立国会図書館
4. アジア歴史資料センター
5. 博物館・自治体史料館
6. 査読論文・研究書
7. 一般向け参考書

## コンテンツ上の禁止事項

- 特定作品の設定や描写を史実として掲載しない。
- 出典のない人物関係を断定しない。
- 後世の著名度を、当時の影響力として表示しない。
- 人物を単純な善悪で説明しない。
- 日付、役職、所属、場所を推測で生成しない。
- 実在人物の発言を、原文の根拠なく引用形式で作らない。

## UI方針

- 初見でも10秒程度で人物の現在の立場を把握できること。
- 各時点の最初の画面に「何が起きているか」「主役」「関係勢力」「転換点」を表示すること。
- 相関図はデスクトップとスマートフォンで同じ図を単純縮小せず、それぞれ読める表現を選ぶこと。
- 詳細情報は段階的に開けること。
- 年代を変更しても、選択中の人物を可能な限り維持すること。
- 人物、勢力、事件、地図の選択状態を連動させること。
- スマートフォン幅320px以上で操作できること。
- キーボード操作とスクリーンリーダーに配慮すること。
- ライトモードとダークモードの両方で確認すること。

## 現在の構成と責務

- `index.html`: 静的な画面構造とスクリプト読込順。
- `src/app.js`: 状態変更、各画面、URLをつなぐエントリーポイント。
- `src/domain.js`: 時点別状態、活動範囲、関係などのドメイン処理。
- `src/state.js` / `src/router.js`: UI状態とURL・ローカル保存。
- `src/renderers/`: 人物、勢力、相関図、事件、共通表示の描画。
- `src/styles.css`: レイアウト、配色、レスポンシブ表示。
- `data/*.json`: 歴史データの正本。編集対象はここだけとする。
- `data.json` / `data.js`: 正本JSONから生成する互換ファイル。直接編集しない。
- `schema/`: 現行互換データと正本JSONのデータ契約。
- `scripts/`: データ生成、検証、公開成果物作成、リンク検査。
- `tests/`: データ、ドメイン、ブラウザー操作、画像、アクセシビリティの回帰テスト。
- `dist/`: `npm run build:site` で生成する公開成果物。編集・コミットしない。

ブラウザーからの直接表示と静的ホスティングを維持するため、ビルド時にアプリケーションコードのバンドルや変換は行わない。

## 校正状態と出典

以下はデータ契約上の整合性を保つための規則であり、既存項目の出典精密化をUI改善より優先するという意味ではない。

- `verified`: 項目の内容を直接支える `evidence.sourceIds` が1件以上必要。
- `needs_review`: 項目単位の根拠を確認中。推測で `verified` に変更しない。
- `disputed`: 複数の見解があり、単一の説明へ断定できない。
- 人物単位の略歴や一般年表を、個別の役職・関係・動機の根拠として自動的に流用しない。
- 出典を追加するときは、表示中の主張を実際に裏付ける箇所があることを確認する。
- 出典本文を確認した場合は、`sources.json` の `locator` に見出し・頁・コマなどの該当箇所を、`contentCheckedAt` に内容確認日を記録する。
- `locator` と `contentCheckedAt` は必ず対で登録し、URLの到達確認だけでは `contentCheckedAt` を更新しない。
- 校正待ちの項目は生成データにも状態を保持し、画面の「出典校正中」表示を消さない。
- 出典URLの到達性だけで内容が検証済みとは判断しない。

## データ整合性

変更時には次を確認する。

- すべての人物IDが一意である。
- すべての事件IDが一意である。
- 関係データの人物IDが存在する。
- 関連地点IDが存在する。
- `startSceneId` が `endSceneId` より後になっていない。
- 表示名の期間が重複していない。
- 出典IDが存在する。
- `verified` の項目に出典IDがある。
- 出典URLがHTTPSで、空欄または明らかな404になっていない。
- `data.json` と `data.js` が正本JSONから再生成された最新状態である。
- 年代変更、検索、相関図、地図が壊れていない。
- 校正状態が正本JSONから画面まで失われていない。

## 変更後の確認

- 歴史データ変更後は `npm run build:data` を実行し、生成された `data.json` と `data.js` も同じ変更に含める。
- データ契約と参照だけを確認する場合は `npm run test:data` を実行する。
- UIやドメイン処理を変更した場合は `npm test` を実行する。
- 公開前は `npm run build:site` を実行し、`dist/` 自体に公開検査を通す。
- 出典URLの到達性は `npm run check:links` で確認する。外部サイト障害と通常テストは区別する。
- 画像スナップショットは、差分画像を確認し、意図した表示変更である場合だけ更新する。
- 320px幅、ライト・ダーク、キーボード操作、WCAG A/AAの既存検査を弱めない。

## 作業方法

- 一度に一つの目的だけを変更する。
- 変更前に現在の実装を理解する。
- 大規模な変更前には計画を提示する。
- 既存機能を削除する場合は理由を説明する。
- 作業後は変更ファイル、確認内容、未確認事項を報告する。
- UI変更では可能ならブラウザーで実際に操作して確認する。
- 歴史データの変更とUIコードの変更は、可能な限り別コミットにする。
- 正本JSONと生成物の関係を逆転させない。
- 公開時はリポジトリ全体ではなく、生成した `dist/` の内容だけを配置する。
